"""Thin benchmark adapters.

Harbor is an integration dependency rather than a package dependency. Import
``NanoGrokBuildAgent`` from :mod:`nano_grok_build.adapter.harbor` only from a
pinned Harbor environment.
"""
